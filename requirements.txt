# EpiCaRO Requirements
# Install: uv pip install -r requirements.txt
# Note: torch must be installed FIRST (uv pip install torch setuptools wheel)

# Core
transformers>=4.45.0
accelerate>=0.34.0
pandas>=2.0.0
pyarrow>=14.0.0

# Math evaluation
sympy>=1.12

# Metrics & Visualization
scikit-learn>=1.3.0
matplotlib>=3.7.0

# Logging
wandb>=0.16.0

# Utilities
tqdm>=4.66.0

# Fast batched inference
vllm>=0.6.0

# Distributed RL training framework
verl>=0.2.0

# NOTE: flash-attn is NOT included.
# Build fails on this environment due to setuptools/torch dependency issues.
# Code automatically falls back to PyTorch SDPA (scaled_dot_product_attention).
# If you want to try installing manually:
#   uv pip install torch setuptools wheel
#   uv pip install flash-attn --no-build-isolation
